# Default values for velero-schedule chart
schedules:
  # Example schedule configuration
  # - name: daily-backup
  #   schedule: "0 1 * * *"  # Daily at 1 AM
  #   template:
  #     ttl: "720h"  # 30 days
  #     includedNamespaces:
  #       - default
  #     excludedNamespaces: []
  #     includedResources: []
  #     excludedResources: []
  #     labelSelector:
  #       matchLabels: {}
  #     snapshotVolumes: true
  #     storageLocation: "default"
  #     volumeSnapshotLocations: ["default"]
  #     hooks:
  #       resources: []

# Global configuration
global:
  storageLocation: "default"
  volumeSnapshotLocations: ["default"]
  defaultTtl: "168h"  # 7 days
  defaultSchedule: "0 2 * * *"  # Daily at 2 AM